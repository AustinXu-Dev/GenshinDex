/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['static.wikia.nocookie.net', 'cdn.wanderer.moe'], // Add any additional domains you need here
  },
};

export default nextConfig;
